[Definition]
# Match POST requests to /start to detect flooding/abuse of the video downloading service.
# Works for both Flask's built-in Werkzeug log format and standard Nginx log format.
failregex = ^<HOST> - - \[[^\]]*\] "POST /start HTTP/[0-9.]+" (200|429|400)

ignoreregex =
